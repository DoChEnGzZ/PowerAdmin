spring:
  datasource:
    name: power_admin
    url: jdbc:mysql://localhost:3306/power_admin
    username: root
    password: c3n14z26boy
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      initial-size: 5
      max-active: 5
      min-idle: 1
      max-wait: 30000
      filter:
        stat:
          enabled: true
          log-slow-sql: true
          slow-sql-log-level: WARN
          slow-sql-millis: 1000